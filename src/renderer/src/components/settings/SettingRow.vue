<script setup lang="ts">
/**
 * A single settings row: title (+ optional description) on the left, a
 * right-aligned `control` slot on the right.
 */
defineProps<{
  title: string
  description?: string
}>()
</script>

<template>
  <div class="setting-row">
    <div class="setting-row-text">
      <div class="setting-row-title">{{ title }}</div>
      <div v-if="description" class="setting-row-desc">{{ description }}</div>
    </div>
    <div class="setting-row-control">
      <slot name="control"></slot>
    </div>
  </div>
</template>

<style scoped>
.setting-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: var(--space-row-x);
  padding: var(--space-row-y) var(--space-row-x);
}
.setting-row-text {
  display: flex;
  flex-direction: column;
  gap: 2px;
  min-width: 0;
}
.setting-row-title {
  font-size: var(--font-row-title);
  font-weight: 600;
  color: var(--text-bright);
}
.setting-row-desc {
  font-size: var(--font-row-desc);
  color: var(--text-secondary);
}
.setting-row-control {
  display: flex;
  align-items: center;
  justify-content: flex-end;
  flex-shrink: 0;
}
</style>
