/** Test-only contract helpers. Production consumers must import the feature root. */
export {
  runGitTransportContract,
  type GitTransportContractHarness,
  type GitTransportRequestRecord,
} from './__tests__/gitTransport.contract'
