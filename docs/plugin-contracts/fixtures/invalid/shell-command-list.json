{
  "schemaVersion": 2,
  "apiVersion": "^1.0.0",
  "id": "acme.shell-list",
  "name": "Plugin shell list",
  "version": "1.0.0",
  "publisher": "acme",
  "permissions": { "shell": { "mode": "allowlist", "commands": ["git"] } },
  "marketplace": { "description": "The Host owns the shell allowlist.", "license": "MIT" },
  "contributes": {
    "views": [{ "id": "main", "kind": "custom", "location": "main", "title": "Main", "entry": "frontend/index.html" }]
  }
}
